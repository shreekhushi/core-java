class EatSureRunner{
	public static void main(String[] args){

		String itemName = "Rajma chawal combo";
		int quantity = 2;
		double price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Chole rice combo";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Dal fry rice";
		quantity = 1;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Dal tadka rice";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Veg khichdi";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Masala khichdi";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Curd rice bowl";
		quantity = 4;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Lemon rice bowl";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Tomato rice bowl";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Bisibele bath";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Veg thali mini";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Veg thali deluxe";
		quantity = 1;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "South indian thali";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "North indian thali";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Mini meals combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Full meals combo";
		quantity = 1;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Veg sambar rice";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Curd meal combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Chapati sabzi combo";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Paratha sabzi combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Paneer curry combo";
		quantity = 1;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Mixed veg curry combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Aloo gobi combo";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Aloo matar combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Baingan bharta combo";
		quantity = 1;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Bhindi masala combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Vegetable korma combo";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Paneer tikka masala combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Veg handi combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Jeera aloo combo";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Veg pulao combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Paneer pulao combo";
		quantity = 1;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Peas pulao combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Veg biryani combo";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Curd biryani combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Tamarind rice combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Coconut rice combo";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Vegetable upma bowl";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Rava kesari combo";
		quantity = 1;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Pongal combo special";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Idli combo meal";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Dosa combo meal";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Set dosa combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Uttapam combo";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Masala dosa combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Plain dosa combo";
		quantity = 1;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Rava dosa combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Onion dosa combo";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Ghee dosa combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Paper dosa combo";
		quantity = 1;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Curd vada combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Medu vada combo";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Veg cutlet combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Vegetable pakora combo";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Paneer pakora combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Veg sandwich combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Grilled veg sandwich combo";
		quantity = 3;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);

		itemName = "Veg burger combo";
		quantity = 2;
		price = EatSure.search(itemName, quantity);
		System.out.println("The priceWithQuantity of " + itemName + " is " + price);
	}
}